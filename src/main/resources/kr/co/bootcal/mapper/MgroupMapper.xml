<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.bootcal.mapper.MgroupMapper">
	<select id="getOneGroup" resultType="kr.co.bootcal.domain.MgroupVO">
		select * from mgroup
		where m_id = #{m_id} and g_name = #{g_name}
	</select>
	<select id="getGroupByNo" resultType="kr.co.bootcal.domain.MgroupListVO">
		select *
		from mgrouplist where g_no = #{g_no}
	</select>
	<select id="getGroupByName" resultType="kr.co.bootcal.domain.MgroupListVO">
		select *
		from mgrouplist where g_name = #{g_name}
	</select>
	<select id="getList" resultType="kr.co.bootcal.domain.MgroupListVO">
		select * from mgrouplist where g_name not like '내 캘린더'
	</select>
	<insert id="insertMyCalGroup">
		insert into mgroup(g_name, m_id, g_grade) values (#{g_name}, #{m_id}, 3)
	</insert>
	<insert id="makeGroupList">
		insert into mgrouplist(g_name, g_intro, g_public) values (#{g_name}, #{g_intro}, #{g_public})
	</insert>
	<insert id="makeGroup">
		insert into mgroup (g_name, m_id, g_grade)
		values (#{g_name}, #{m_id}, 3);
	</insert>
	<delete id="deleteGroup">
		delete from mgrouplist where g_no = #{g_no}
	</delete>
</mapper>